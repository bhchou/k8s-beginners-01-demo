apiVersion: v1
kind: Service

metadata:
  name: python-demo
  namespace: k8s-demo

spec:
  selector:
    app: python-demo

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP
